<nav class="top-bar top-bar-link-bg-hover-lightness" data-topbar role="navigation">
  <ul class="title-area">
    <li class="name">
      <h1><%= link_to "Workout App", root_path%></h1>
    </li>
    <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
  </ul>

  <section class="top-bar-section">
    <ul class="right">
      <li class="has-dropdown not-click">
        <a href="#"> Coaches </a>
        <ul class="dropdown">
          <% User.all.select{|x| x.roles.include?(Role.find_by(role: "Coach"))}.sort{|x,y| x.name <=> y.name}.each do |user| %>
            <li><%= link_to user.name, user_path(user) %></li>
          <% end %>
        </ul>
      </li>
      <li class="has-dropdown not-click">
        <a href="#"> Athletes </a>
        <ul class="dropdown">
          <% User.all.select{|x| x.roles.include?(Role.find_by(role: "Athlete"))}.sort{|x,y| x.name <=> y.name}.each do |user| %>
            <li><%= link_to user.name, user_path(user) %></li>
          <% end %>
        </ul>
      </li>
    </ul>
    <ul class="left">
      <li><%= link_to "Exercise Categories", categories_path %></li>
    </ul>
  </section>

</nav>